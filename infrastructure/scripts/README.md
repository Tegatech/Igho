# Infrastructure scripts

Operational and migration helper scripts live here.

Scripts must fail fast, avoid embedded secrets, document required environment variables, and produce useful non-sensitive logs.
