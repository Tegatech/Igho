/**
 * Production web application boundary.
 *
 * M0 intentionally does not lock the final web framework.
 * M1 will establish the runtime alongside the auth/session integration.
 */
export const appName = "Igho";
